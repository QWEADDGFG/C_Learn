#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

/**********************************************************************
 *                          COMMENT                                   *
 **********************************************************************/

int main(void) {
	// int i, j;
	// float x, y;

	// 返回匹配成功的转换说明的个数
	// int n = scanf("%d%d%f%f", &i, &j, &x, &y);
	
	// int i, j;
	// scanf("%d/%d", &i, &j);

	// 不要混淆printf和scanf的用法
	// int i = 10, j = 20;
	// printf("i = %d, j = %d\n", &i, &j);
	int i, j;
	// scanf("i = %d, j = %d", &i, &j);
	scanf("%d\n", &i);

	return 0;
}